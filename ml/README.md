# ML workspace

Bu klasör, model eğitimi/deneme dosyaları için ayrılmıştır.

- Mevcut örnek: `vegetable-classification/` (dataset + eğitim scriptleri + `.h5/.keras`).
- Üretim servis entegrasyonu: `backend/app/ml/` altında modüler servis yapısı.

